# Development / test dependencies.
#
# Install alongside the runtime dependencies with:
#   python -m pip install -r requirements-dev.txt

-r requirements.txt

# Required by fastapi.testclient.TestClient (test/tests/test_web_ui.py).
httpx>=0.24.0

# Test runner and repository lint command documented in README.md.
pytest>=8.0.0
ruff>=0.8.0
